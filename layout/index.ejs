<section class="main clearfix">
    <% page.posts.each(function(item){ %>
        <%- partial('_partial/article-index-portfolio', {item: item}) %>
    <% }); %>
    <%- partial('_partial/pagination') %>
</section>